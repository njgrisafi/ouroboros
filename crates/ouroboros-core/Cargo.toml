[package]
name = "ouroboros-core"
version = "0.3.1"
edition = "2024"

[dependencies]
rustpython-parser = "0.4"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"