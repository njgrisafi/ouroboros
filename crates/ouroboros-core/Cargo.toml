[package]
name = "ouroboros-core"
version = "0.7.1"
edition = "2024"

[dependencies]
rustpython-ast = { version = "0.4", features = ["visitor"] }
rustpython-parser = "0.4"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
toml_edit = "0.22"

[dev-dependencies]
tempfile = "3"
