# Module-level string candidate: closes the a <-> b cycle when
# string imports are enabled.
TARGET = "my.app.a"

# Unresolvable candidate: silently dropped (not an unresolved import).
NOTE = "not.a.module"
